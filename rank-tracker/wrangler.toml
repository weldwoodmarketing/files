name = "weldwood-rank-tracker"
pages_build_output_dir = "public"
compatibility_date = "2024-11-01"

# Non-secret config. The Semrush key is a SECRET — never put it here.
# Set it with:  npx wrangler pages secret put SEMRUSH_API_KEY
[vars]
CACHE_TTL_HOURS = "12"
# ALLOW_ORIGIN = "https://rank.weldwoodmarketing.com"  # optional; defaults to same-origin
